function countdown(secondsRemaining) {var days = Math.floor(secondsRemaining / 86400),hours = Math.floor((secondsRemaining - (days * 86400)) / 3600),minutes = Math.floor((secondsRemaining - (days * 86400) - (hours * 3600)) / 60),seconds = secondsRemaining - (days * 86400) - (hours * 3600) - (minutes * 60);if(secondsRemaining > 0) {if(days < 10) { days = '0' + days; }if(hours < 10) { hours = '0' + hours; }if(minutes < 10) { minutes = '0' + minutes; }if(seconds < 10) { seconds = '0' + seconds; }$('.days > .number').html(days);$('.hours > .number').html(hours);$('.minutes > .number').html(minutes);$('.seconds > .number').html(seconds);secondsRemaining--;} else {if(secondsRemaining == 0) {window.location.reload();}}window.setTimeout(function() {countdown(secondsRemaining);}, 1000);}
function updateProgress(out) {procent=(100-100/(out['open']-out['close'])*(out['open']-Math.floor(new Date().getTime()/1000))).toFixed(3); if(procent <= 7) $('.progressBarFill').css({ width: '7%' }).find('.progressBarFillMiddle').html('10%');else if(procent >= 100) $('.progressBarFill').css({ width: '100%' }).find('.progressBarFillMiddle').html('100%');else $('.progressBarFill').css({ width: procent + '%' }).find('.progressBarFillMiddle').html(procent + '%');window.setTimeout(function() {updateProgress({'close':out['close'],'open':out['open']});}, 1000);}
function objectConverter(array) {var object = {};for(var i=0; i<array.length; i++) {object[array[i]] = '';}return object;}
function changeContainerPosition() { var windowHeight = $(window).height(); var containerHeight = $('.container').height() - $('.container > .logo').height() + $('.container > .bottomBar').height(); var calculate = (windowHeight - containerHeight) / 2; calculate -= $('.container > .logo').height(); calculate += $('.container > .bottomBar').height(); if(calculate >= 0) { $('.container').css({ margin: calculate + 'px auto 0 auto' }); } }